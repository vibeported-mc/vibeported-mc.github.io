# Dependencies
node_modules/

# Vite build output — the deploy workflow builds this, it is never committed
dist/
.vite/

# Generated release index. Regenerated by `npm run releases` and by the deploy
# workflow, so the committed tree stays free of machine-written data.
public/releases.json

# Logs and caches
*.log
npm-debug.log*
.npm/
.eslintcache

# Editors
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini
